<!DOCTYPE html>
<html lang="es">
<head>
	<meta charset="utf-8">
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>CEFP</title>

	<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">

	<link rel="preconnect" href="https://fonts.googleapis.com">
	<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
	<link href="https://fonts.googleapis.com/css2?family=Afacad&display=swap" rel="stylesheet">

</head>
<body>

	<div class="container">

		<h1>CEFP</h1>
        <p>Edicion de solicitud de <b>{{alumno.nombre}}</b></p>
        <a href="index.php?m=Alumno&f=logout">Salir</a> | <a href="index.php?m=Solicitud&f=showSolicitudes">Gestionar Solicitudes del Alumno</a> | <a href="index.php?m=Solicitud&f=showSolicitudes">Volver</a>
	</div>
    <form action="index.php" method="post">
        <input type="hidden" name="m" value="Solicitud">
        <input type="hidden" name="f" value="editar">
        <input type="hidden" name="idAlu" value="{{solicitud.alumno.idAlu}}">
        <input type="hidden" name="id" value="{{solicitud.idSol}}">

        <label for="profesor">Profesor</label>
        <select name="profesor" id="profesor">
            <option disabled selected>Selecciona profesor</option>
            {% for profesor in profesores %}
                {% if solicitud.profesor.nombre == profesor.nombre %}
                    <option selected value="{{profesor.idPro}}">{{profesor.nombre}}</option>
                
                {% else %}
                    <option value="{{profesor.idPro}}">{{profesor.nombre}}</option>    
                {% endif %}
            {% endfor %}
            
        </select><br>

        <label for="modulo">Modulo Colaboracion</label>
        <select name="modulo" id="modulo">
            <option disabled selected>Selecciona modulo</option>
            {% for modulo in modulos %}
                {% if solicitud.modulo.modulo == modulo.modulo %}
                    <option selected value="{{modulo.idMod}}">{{modulo.modulo}}</option>    
                {% else %}
                    <option value="{{modulo.idMod}}">{{modulo.modulo}}</option>    
                {% endif %}
            {% endfor %}
            <option value=""></option>
        </select><br>

        <label for="cv">Curriculum Vitae: </label><br>
        <p>Introduce una breve informacion sobre tu formacion e itinerario laboral</p><br>
        <textarea name="cv" id="cv" cols="80" rows="10">{{solicitud.cv}}</textarea><br>

        <input type="submit" value="Realizar Solicitud">

    </form>

</body>
</html>